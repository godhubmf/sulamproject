RewriteEngine On

# If the file or folder actually exists, use it.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Otherwise, send everything to index.php
RewriteRule ^ index.php [QSA,L]